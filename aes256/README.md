## Para rodar o programa

##### No terminal:

` cargo run -- testar`: comando para testar com o NIST.
`cargo run -- cifrar mensagem.txt`: comando para cifrar uma mensagem que esteja na raiz do projeto, `cripto-sec/aes256`.
`cargo run -- decifrar mensagem.txt.cifrado`: comando para decifrar a mensagem que foi cifrada.
